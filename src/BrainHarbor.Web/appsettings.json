{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",

  "//Publishing": "M3 rollout gate. Ships in Review (a person approves every summary) so the first real AI summaries are all human-checked. Flip to Auto when confident — no code change, just this value. See docs/content-pipeline.md 'Publish mode'.",
  "Publishing": {
    "Mode": "Review"
  }
}
