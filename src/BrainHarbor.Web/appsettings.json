{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",

  "//Publishing": "Auto: a summary that passes every automated safety check publishes itself; anything a check flags is held. The site does not claim human review. See docs/content-pipeline.md 'Publish mode'.",
  "Publishing": {
    "Mode": "Auto"
  }
}
